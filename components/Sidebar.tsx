/**
 * Sidebar.tsx — ARŞİVLENDİ
 *
 * Bu dosya /dashboard kaldırıldığında işlevsiz kaldı.
 * Kullanıldığı yer: app/(main)/dashboard/layout.tsx (arşivde)
 *
 * DURUM: Güvenle silebilirsiniz. Dashboard yeniden eklenirse
 * sıfırdan daha temiz yazılacak.
 *
 * Silmeden önce projenin hiçbir yerinde import edilmediğinden
 * emin olun:
 *   grep -r "from.*Sidebar" ./app ./components
 */

export default function Sidebar() {
  return null;
}